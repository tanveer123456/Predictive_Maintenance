from setuptools import find_packages,setup

setup(
    name="PredictiveMaintenance",
    version="0.0.1",
    author="Tanveer",
    packages=find_packages() #"Find all Python packages in this project automatically."
)